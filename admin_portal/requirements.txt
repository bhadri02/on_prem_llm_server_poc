# admin_portal/requirements.txt
# Admin Portal (Layer 10) — pinned runtime dependencies.
# API backend that proxies to other services — no ML packages.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings
pydantic-settings==2.6.1

# Async HTTP client (calls to api-gateway, audit-store, model-registry)
httpx==0.27.2

# Prometheus metrics exposition
prometheus-client==0.21.1

# Structured logging (shared observability module dependency)
structlog==24.4.0
