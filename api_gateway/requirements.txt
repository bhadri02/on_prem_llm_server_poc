# api_gateway/requirements.txt
# API Gateway (Layer 1) — pinned runtime dependencies.
# Entry point service — auth, rate limiting, and forwarding. No ML packages.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings & validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Async HTTP client (proxy forwarding to security layer)
httpx==0.27.2

# Redis client (per-key rate limit counters — shared across replicas)
redis[asyncio]==5.0.8

# Prometheus metrics exposition
prometheus-client==0.21.1

# Structured logging (shared observability module dependency)
structlog==24.4.0
