# audit_store/requirements.txt
# Audit Store — pinned runtime dependencies.
# No ML or heavy packages.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings
pydantic-settings==2.6.1

# Prometheus metrics exposition
prometheus-client==0.21.1

# Structured logging (shared observability module dependency)
structlog==24.4.0

# Audit trail storage (Postgres via SQLAlchemy — same versions as admin_portal)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
