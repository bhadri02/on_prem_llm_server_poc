# inference_adapter/requirements.txt
# Inference Adapter (Layer 5) — pinned runtime dependencies.
# Thin HTTP bridge to Ollama — no ML libraries needed here.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings
pydantic-settings==2.6.1

# Async HTTP client (calls to Ollama)
httpx==0.27.2

# Prometheus metrics exposition
prometheus-client==0.21.1

# Structured logging (shared observability module dependency)
structlog==24.4.0
