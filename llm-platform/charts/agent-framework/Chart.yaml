apiVersion: v2
name: agent-framework
description: >
  Agent Framework (Layer 6 — POC). A LangGraph ReAct agent service that handles
  agentic requests from the Intelligent Router, runs tool-calling loops with up to
  MAX_AGENT_STEPS iterations, and returns a fully-populated IMF response.
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - llm
  - agent
  - langgraph
  - react
home: https://github.com/your-org/on_prem_llm_server_poc
maintainers:
  - name: Platform Team
    email: platform@your-org.com
