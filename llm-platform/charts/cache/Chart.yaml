apiVersion: v2
name: cache
description: "Cache Layer (Layer 4) — exact and semantic caching for the LLM platform"
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: redis
    version: "19.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
