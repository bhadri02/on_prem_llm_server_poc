[pytest]
testpaths = tests/helm
addopts = --tb=short
markers =
    pbt: property-based tests using Hypothesis
    unit: unit tests for individual chart compliance
    integration: integration tests requiring a live cluster or helm binary
    smoke: smoke tests for quick sanity checks of default values
