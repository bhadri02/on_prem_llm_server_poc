"""
Middleware sub-package for the Model Registry.

Contains the AuthMiddleware (X-API-Key enforcement on mutating endpoints)
and the LoggingMiddleware (structured JSON request logging to stdout).
"""
