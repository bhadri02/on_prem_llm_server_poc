# requirements.txt — Root-level consolidated dependencies for local development
# Install once in your venv from the repo root: pip install -r requirements.txt
# This covers all 7 core services + admin_portal + agent_framework.
# Per-service requirements.txt files remain for Docker builds.

# ── Web framework (shared by all services) ─────────────────────────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1

# ── Settings & validation ───────────────────────────────────────────────────
pydantic>=2.10.3
pydantic-settings>=2.6.1

# ── HTTP client ─────────────────────────────────────────────────────────────
httpx>=0.27.2

# ── Async support ───────────────────────────────────────────────────────────
anyio>=4.7.0

# ── YAML parsing (router, security_layer) ──────────────────────────────────
pyyaml>=6.0.2

# ── Observability ───────────────────────────────────────────────────────────
prometheus-client>=0.21.1
structlog>=24.4.0

# ── Admin Portal — users/roles/API keys DB (Postgres via SQLAlchemy) ────────
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10

# ── Cache Service ───────────────────────────────────────────────────────────
redis[asyncio]>=5.0.8
sentence-transformers>=3.3.1

# ── Security Layer — PII detection (heavy; needs C compiler or wheels) ──────
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
spacy>=3.7.5
# After installing spacy run: python -m spacy download en_core_web_sm

# ── Agent Framework (LangGraph) ─────────────────────────────────────────────
langchain-openai>=0.3.18
langgraph>=0.4.7
langchain-core>=0.3.61

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.3.5
pytest-asyncio>=0.24.0
pytest-anyio
hypothesis>=6.131.18
pytest-httpx>=0.30.0
respx>=0.22.0  # 0.21.x silently fails to match any request under httpx>=0.28
fakeredis[aioredis]
