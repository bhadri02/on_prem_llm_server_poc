# Agent Framework (Layer 6) — pinned production dependencies
# Python 3.12 required

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Settings management
pydantic-settings==2.9.1

# LangChain / LangGraph agent stack
langchain-openai==0.3.18
langgraph==0.4.7
langchain-core==0.3.61

# HTTP client (used by LangChain internals and direct health checks)
httpx==0.28.1

# Observability
prometheus-client==0.21.1

# Configuration / serialization
pyyaml==6.0.2

# Shared observability dependencies (shared.observability imports these)
structlog==24.4.0
starlette==0.41.3

# Distributed tracing (opt-in via TRACING_ENABLED — shared.observability.middleware
# gracefully no-ops if these aren't installed, but they must be present for
# TRACING_ENABLED=true to actually do anything)
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
