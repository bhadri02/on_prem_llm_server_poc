# shared/requirements.txt
# Shared observability package — minimal runtime dependencies.
# Installed alongside every service's own requirements.txt.

# Structured JSON logging
structlog==24.4.0

# Prometheus metrics (Counter, Histogram, etc.)
prometheus-client==0.21.1

# Starlette (BaseHTTPMiddleware used in middleware.py)
# Note: already pulled in transitively by fastapi in every service,
# but listed here so the shared package is self-contained.
starlette==0.41.3
