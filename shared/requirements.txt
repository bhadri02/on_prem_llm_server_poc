# shared/requirements.txt
# Pinned dependencies for the shared observability package.
# All six platform layers should install this file alongside their own requirements.
#
# Install (editable) alongside a layer:
#   pip install -r shared/requirements.txt
#   pip install -e shared/   # once setup.py / pyproject.toml is added

structlog==24.4.0
prometheus-client==0.20.0

# Testing
hypothesis==6.108.5
pytest==8.2.2
